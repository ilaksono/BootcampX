SELECT cohorts.name, COUNT(assignment_submissions.*) from assignment_submissions JOIN students ON (students.id = student_id) JOIN cohorts ON (cohort_id = cohorts.id) GROUP BY cohorts.name ORDER BY COUNT(assignment_submissions.*) DESC;
